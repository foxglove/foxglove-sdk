[project]
name = "foxglove-sdk-monorepo"
version = "0.0.0"
requires-python = ">=3.10"

[dependency-groups]
dev = [
    "flake8>=7.1.1,<8",
    "black>=26.3.1,<27",
    "isort>=9.0.0,<10",
]

[tool.isort]
profile = "black"

[tool.uv]
package = false
