// Generated by https://github.com/foxglove/foxglove-sdk

syntax = "proto3";

import "foxglove/CameraCalibration.proto";
import "foxglove/Grid.proto";
import "foxglove/LaserScan.proto";
import "foxglove/PointCloud.proto";
import "foxglove/PoseInFrame.proto";
import "foxglove/PosesInFrame.proto";
import "foxglove/SceneEntity.proto";
import "foxglove/VoxelGrid.proto";
import "google/protobuf/timestamp.proto";

package foxglove;

// Information about a selected entity in a visualization panel
message SelectedEntity {
  // Timestamp of the selection
  google.protobuf.Timestamp timestamp = 1;

  // Frame of reference for the selected entity
  string frame_id = 2;

  // Topic from which the entity originated
  string source_topic = 3;

  // Schema name of the source message
  string source_schema_name = 4;

  // Identifier of the selected entity
  string entity_id = 5;

  // Selected scene entity
  optional foxglove.SceneEntity scene_entity = 6;

  // Selected point cloud
  optional foxglove.PointCloud point_cloud = 7;

  // Selected laser scan
  optional foxglove.LaserScan laser_scan = 8;

  // Selected grid
  optional foxglove.Grid grid = 9;

  // Selected voxel grid
  optional foxglove.VoxelGrid voxel_grid = 10;

  // Selected camera calibration
  optional foxglove.CameraCalibration camera_calibration = 11;

  // Selected pose in frame
  optional foxglove.PoseInFrame pose_in_frame = 12;

  // Selected poses in frame
  optional foxglove.PosesInFrame poses_in_frame = 13;
}
