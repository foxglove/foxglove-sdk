// Generated by https://github.com/foxglove/foxglove-sdk

syntax = "proto3";

import "foxglove/CameraCalibration.proto";
import "foxglove/Grid.proto";
import "foxglove/KeyValuePair.proto";
import "foxglove/LaserScan.proto";
import "foxglove/PointCloud.proto";
import "foxglove/PoseInFrame.proto";
import "foxglove/PosesInFrame.proto";
import "foxglove/SceneEntity.proto";
import "foxglove/VoxelGrid.proto";

package foxglove;

// Information about a selected entity in a visualization panel
message SelectedEntity {
  // Topic from which the entity originated
  string source_topic = 1;

  // Schema name of the source message
  string source_schema_name = 2;

  // Selected scene entity
  optional foxglove.SceneEntity scene_entity = 3;

  // Selected point cloud
  optional foxglove.PointCloud point_cloud = 4;

  // Selected laser scan
  optional foxglove.LaserScan laser_scan = 5;

  // Selected grid
  optional foxglove.Grid grid = 6;

  // Selected voxel grid
  optional foxglove.VoxelGrid voxel_grid = 7;

  // Selected camera calibration
  optional foxglove.CameraCalibration camera_calibration = 8;

  // Selected pose in frame
  optional foxglove.PoseInFrame pose_in_frame = 9;

  // Selected poses in frame
  optional foxglove.PosesInFrame poses_in_frame = 10;

  // Additional user-provided metadata associated with the selected entity.
  repeated foxglove.KeyValuePair metadata = 11;
}
