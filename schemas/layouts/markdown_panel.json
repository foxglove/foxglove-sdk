{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MarkdownPanel",
  "description": "Displays Github flavored Markdown within a layout. Check out [the documentation](https://docs.foxglove.dev/docs/visualization/panels/markdown) for more information.",
  "allOf": [{ "$ref": "./base_panel.json" }],
  "properties": {
    "type": {
      "const": "Markdown",
      "default": "Markdown"
    },
    "config": {
      "$ref": "#/$defs/MarkdownConfig"
    }
  },
  "required": ["config"],
  "$defs": {
    "MarkdownConfig": {
      "type": "object",
      "properties": {
        "markdown": {
          "type": "string",
          "description": "The Markdown content to display"
        },
        "fontSize": {
          "description": "The font size to use for the Markdown content",
          "enum": [8, 9, 10, 11, 12, 14, 16, 18, 24, 30, 36, 48, 60, 72],
          "default": 12
        },
        "foxglovePanelTitle": {
          "type": "string",
          "description": "The title of the panel in Foxglove"
        }
      }
    }
  }
}
