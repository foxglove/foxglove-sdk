[project]
name = "foxglove-sdk-examples"
version = "0.0.0"
requires-python = ">=3.9"

[tool.uv]
package = false

[tool.uv.workspace]
members = [ "*" ]

[build-system]
requires = ["uv_build>=0.9.3,<0.10.0"]
build-backend = "uv_build"
