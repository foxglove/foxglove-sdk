/**
 * Foxglove message type definitions for TypeScript.
 *
 * This package provides the same types as `@foxglove/schemas`. In a future version,
 * this will become the canonical location for these types and `@foxglove/schemas`
 * will re-export from this package.
 *
 * @packageDocumentation
 */

// Re-export everything from @foxglove/schemas.
export * from "@foxglove/schemas";
