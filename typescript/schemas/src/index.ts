/**
 * @deprecated This package has been renamed to `@foxglove/messages`.
 * Please update your imports:
 *
 * ```diff
 * - import { CompressedImage } from "@foxglove/schemas";
 * + import { CompressedImage } from "@foxglove/messages";
 * ```
 *
 * This package will continue to work but may be removed in a future major version.
 *
 * @packageDocumentation
 */

// Re-export everything from @foxglove/messages for backward compatibility.
export * from "@foxglove/messages";
